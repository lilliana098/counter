import fontawesome from '@fortawesome/fontawesome'
import faClock from '@fortawesome/fortawesome-free-regular/faCloc'

FontAwesome.config = {
    autoReplaceSvg: 'nest'
}

fontawesome.library.add(faClock);